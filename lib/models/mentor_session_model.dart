// Model types for the Mentor Avatar Session feature.

/// Voice vs text interaction mode.
enum MentorSessionMode { voice, text }

/// Visual animation state of the 3D mentor avatar.
enum MentorAvatarState { idle, listening, thinking, speaking }
