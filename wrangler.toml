name = "weltenbibliothek"
pages_build_output_dir = "build/web"

# Cloudflare Pages compatibility
compatibility_date = "2024-01-01"

# Environment variables für Build
[env.production]
vars = { FLUTTER_WEB_RENDERER = "canvaskit" }

# Asset handling
[site]
bucket = "build/web"
